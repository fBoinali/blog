{% extends 'admin/base_admin.html.twig' %}

{% block title %}Team index{% endblock %}

{% block body %}
    <h1>Team index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr>
                <td>{{ team.id }}</td>
                <td>{{ team.email }}</td>
                <td>{{ team.roles ? team.roles|json_encode : '' }}</td>
                <td>{{ team.password }}</td>
                <td>{{ team.firstname }}</td>
                <td>{{ team.lastname }}</td>
                <td>
                    <a href="{{ path('app_team_admin_show', {'id': team.id}) }}">show</a>
                    <a href="{{ path('app_team_admin_edit', {'id': team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_team_admin_new') }}">Create new</a>
{% endblock %}
